apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-frontend.fullname" . }}
  labels:
    {{- include "mcp-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mcp-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
